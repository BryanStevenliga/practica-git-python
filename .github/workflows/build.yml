name: Python RPG Build with Docker

on:
  push:
    branches: [ main, feature-* ]
  pull_request:
    branches: [ main ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

<<<<<<< HEAD
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies (Instalar paquetes)
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Compile / Syntax check
      run: |
        python -m py_compile personaje.py enemigo.py principal.py
=======
    - name: Build Docker Image (Compilar paquete Docker)
      run: |
        docker build -t python-rpg-game:latest .

    - name: Run Game in Docker Container (Probar contenedor)
      run: |
        docker run --rm python-rpg-game:latest
>>>>>>> 2ab1190 (feat: agregando los packages)
